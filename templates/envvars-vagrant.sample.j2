export RAKE_ANSIBLE_USE_VAGRANT=1
export {{ role_prefix|upper }}_{{ role_name|upper }}_VAGRANT_BOXNAME=ubuntu/trusty64 # name of the vagrant box to use for testing
export {{ role_prefix|upper }}_{{ role_name|upper }}_VAGRANT_PROVIDER=virtualbox     # name of the vagrant provider to use for testing
